<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comin.example.root.kel3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Percabangan" />
        <activity android:name=".If" />
        <activity android:name=".Perulangan" />
        <activity android:name=".IfElse" />
        <activity android:name=".IfElse2" />
        <activity android:name=".SwCase" />
        <activity android:name=".For" />
        <activity android:name=".While" />
        <activity android:name=".DoWhile" />
        <activity android:name=".Array"></activity>
    </application>

</manifest>